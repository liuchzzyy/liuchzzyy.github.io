# 项目架构文档

本文档详细说明 PRISM 项目的技术架构和设计原则。

## 🏗️ 整体架构

PRISM 采用 **三层架构** 设计：

```
┌─────────────────────────────────────────────────────┐
│                   内容层 (Content)                    │
│  TOML 配置 + Markdown 内容 + BibTeX 数据              │
└─────────────────┬───────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────┐
│              解析和类型层 (Parsing & Types)           │
│  配置解析器 + 内容加载器 + BibTeX 解析器 + TS 类型    │
└─────────────────┬───────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────┐
│                 渲染层 (Rendering)                    │
│  React 组件 + Next.js 路由 + 静态生成                │
└─────────────────────────────────────────────────────┘
```

## 📂 目录结构详解

### 根目录文件

```
PRISM/
├── .editorconfig           # 编辑器配置，统一代码风格
├── .nvmrc                  # Node.js 版本要求 (22+)
├── .gitignore              # Git 忽略规则
├── package.json            # 项目依赖和脚本
├── tsconfig.json           # TypeScript 配置
├── next.config.ts          # Next.js 配置（静态导出）
├── tailwind.config.mjs     # Tailwind CSS 配置
├── postcss.config.mjs      # PostCSS 配置
├── eslint.config.mjs       # ESLint 代码规范配置
├── LICENSE                 # MIT 许可证
├── README.md               # 项目说明文档
├── CHANGELOG.md            # 版本更新日志
└── CONTRIBUTING.md         # 贡献指南
```

### 内容目录 (`content/`)

**用途**: 存放所有用户可编辑的内容和配置

```
content/
├── README.md               # 内容管理说明
├── config.toml             # 全站配置（英文）
├── config.zh.toml          # 全站配置（中文）
├── about.toml              # 首页配置（英文）
├── about.zh.toml           # 首页配置（中文）
├── publications.bib        # BibTeX 论文数据
├── *.toml                  # 各页面配置文件
├── *.zh.toml               # 中文版页面配置
├── *.md                    # Markdown 内容文件
└── *.zh.md                 # 中文版内容文件
```

**设计原则**:
- 配置驱动：所有内容通过配置文件管理
- 多语言支持：`.zh` 后缀表示中文版本
- 类型安全：配置文件结构由 TypeScript 类型约束

### 源代码目录 (`src/`)

**用途**: 核心应用代码

#### `src/app/` - Next.js 应用路由

```
app/
├── layout.tsx              # 根布局（全局样式、主题提供者）
├── page.tsx                # 首页路由
├── globals.css             # 全局 CSS（Tailwind 基础）
└── [slug]/
    └── page.tsx            # 动态路由（所有其他页面）
```

**路由机制**:
- 首页 (`/`) → `page.tsx`
- 其他页面 (`/[slug]`) → `[slug]/page.tsx`
- 使用 `generateStaticParams()` 预生成所有页面

#### `src/components/` - React 组件

```
components/
├── ClientHome.tsx          # 首页客户端容器
├── ClientDynamicPage.tsx   # 动态页面客户端容器
├── home/                   # 首页专用组件
│   ├── Profile.tsx         # 个人信息卡片
│   ├── About.tsx           # 关于部分
│   ├── News.tsx            # 新闻列表
│   └── SelectedPublications.tsx # 精选论文
├── layout/                 # 布局组件
│   ├── Navigation.tsx      # 导航栏
│   └── Footer.tsx          # 页脚
├── pages/                  # 页面类型组件
│   ├── TextPage.tsx        # 文本页面（Markdown）
│   ├── CardPage.tsx        # 卡片页面（项目/新闻）
│   └── ListPage.tsx        # 列表页面
├── publications/           # 论文相关组件
│   └── PublicationsList.tsx # 论文列表渲染
└── ui/                     # 通用 UI 组件
    ├── ThemeProvider.tsx   # 主题提供者
    ├── ThemeToggle.tsx     # 主题切换按钮
    └── LanguageToggle.tsx  # 语言切换按钮
```

**组件设计原则**:
- 职责单一：每个组件专注于特定功能
- 类型安全：使用 TypeScript 接口定义 props
- 可复用性：通用组件放在 `ui/` 目录

#### `src/lib/` - 工具库

```
lib/
├── config.ts               # 配置加载器
├── content.ts              # 内容加载器（通用）
├── bibtexParser.ts         # BibTeX 解析器
├── utils.ts                # 通用工具函数
├── i18n/                   # 国际化支持
│   ├── translations.ts     # UI 文本翻译
│   └── useTranslation.ts   # 翻译 Hook
└── stores/                 # 状态管理（Zustand）
    ├── themeStore.ts       # 主题状态
    └── languageStore.ts    # 语言状态
```

**核心功能**:
- **config.ts**: 解析 `config.toml`，提供 `getConfig()` 函数
- **content.ts**: 提供 `getPageConfig()`, `getMarkdownContent()` 等加载函数
- **bibtexParser.ts**: 将 BibTeX 转换为 `Publication` 类型数组

#### `src/types/` - TypeScript 类型定义

```
types/
├── page.ts                 # 页面配置类型
├── publication.ts          # 论文类型
└── bib.d.ts                # BibTeX 模块声明
```

**类型系统**:
- `BasePageConfig`: 所有页面的基础接口
- `TextPageConfig`: 文本类型页面
- `CardPageConfig`: 卡片类型页面
- `Publication`: 论文数据结构

### 文档目录 (`docs/`)

**用途**: 项目文档和静态资源

```
docs/
├── README.md               # 文档索引
├── guides/                 # 用户指南
│   ├── deployment.md       # 部署指南
│   └── i18n.md             # 国际化说明
└── assets/                 # 文档资源
    ├── images/             # 图片（Logo、截图、头像等）
    ├── papers/             # 论文相关文件
    └── projects/           # 项目图片
```

**资源同步**:
- 构建时 `scripts/sync-assets.js` 将 `docs/` 复制到 `public/docs/`
- 确保静态资源在部署后可访问

### 脚本目录 (`scripts/`)

```
scripts/
├── README.md               # 脚本说明文档
└── sync-assets.js          # 资源同步脚本
```

### GitHub 配置 (`.github/`)

```
.github/
├── copilot-instructions.md # AI 辅助开发指南
└── workflows/
    └── deploy.yml          # GitHub Actions 部署工作流
```

## 🔄 数据流

### 1. 构建时数据流

```
content/*.toml
    ↓ (TOML 解析)
SiteConfig / PageConfig
    ↓ (类型验证)
React Components
    ↓ (Next.js SSG)
静态 HTML (out/)
```

### 2. 页面渲染流程

```
用户访问 URL
    ↓
Next.js 路由匹配
    ↓
[首页] page.tsx → ClientHome → 多个 home/* 组件
[其他] [slug]/page.tsx → ClientDynamicPage → pages/* 组件
    ↓
getPageConfig() 加载配置
    ↓
根据 type 选择对应组件
    ↓
渲染页面内容
```

### 3. 内容加载流程

```
getPageConfig(slug, lang)
    ↓
尝试加载 {slug}.{lang}.toml
    ↓ (失败)
回退到 {slug}.toml
    ↓
解析 TOML 配置
    ↓
根据 type 加载相应内容
  ├─ text → getMarkdownContent()
  ├─ publication → parseBibTeX()
  └─ card → 直接从配置获取
```

## 🎨 技术栈

### 核心框架
- **Next.js 15.3** - React 框架，提供路由和静态生成
- **React 19** - UI 组件库
- **TypeScript 5.9** - 类型安全

### 样式
- **Tailwind CSS 4** - 实用优先的 CSS 框架
- **PostCSS** - CSS 处理工具
- **Framer Motion** - 动画库

### 状态管理
- **Zustand** - 轻量级状态管理（主题、语言）
- **next-themes** - 主题切换支持

### 内容解析
- **smol-toml** - TOML 解析器
- **bibtex-parse-js** - BibTeX 解析器
- **react-markdown** - Markdown 渲染

### 开发工具
- **ESLint** - 代码规范检查
- **TypeScript Compiler** - 类型检查

## 🔧 构建流程

### 开发模式 (`npm run dev`)

```bash
1. node scripts/sync-assets.js  # 同步资源
2. next dev --turbopack          # 启动开发服务器（使用 Turbopack）
```

### 生产构建 (`npm run build`)

```bash
1. node scripts/sync-assets.js  # 同步资源
2. next build                    # 构建应用
   ├─ TypeScript 类型检查
   ├─ ESLint 代码检查
   ├─ 编译 React 组件
   ├─ 生成静态页面 (SSG)
   └─ 导出到 out/ 目录
```

### 静态导出配置

`next.config.ts` 中的关键配置:

```typescript
{
  output: 'export',           // 启用静态导出
  trailingSlash: true,        // URL 末尾添加斜杠
  images: {
    unoptimized: true         // 禁用图片优化（静态部署需要）
  }
}
```

## 🌍 国际化 (i18n) 实现

### 方案
- **配置级**: 每个配置文件有 `.zh` 版本
- **UI 级**: `translations.ts` 提供 UI 文本翻译
- **状态级**: `languageStore` 管理当前语言

### 语言切换流程

```
用户点击语言切换按钮
    ↓
更新 languageStore
    ↓
localStorage 保存偏好
    ↓
组件重新渲染
    ↓
getPageConfig(slug, newLang)
    ↓
加载对应语言的配置
```

## 🎯 性能优化策略

1. **静态生成 (SSG)**: 所有页面在构建时生成 HTML
2. **代码分割**: Next.js 自动按路由分割代码
3. **图片优化**: 使用 Next.js Image 组件（开发时）
4. **CSS 优化**: Tailwind 自动删除未使用的样式
5. **最小化 JavaScript**: 仅必要的客户端交互

## 🔒 安全考虑

1. **类型安全**: TypeScript 提供编译时类型检查
2. **内容安全**: Markdown 渲染使用 react-markdown（防止 XSS）
3. **依赖安全**: 定期更新依赖，使用 `npm audit`
4. **无服务器**: 纯静态站点，无后端攻击面

## 📦 部署流程

### 手动部署

```bash
npm run build    # 生成 out/ 目录
# 将 out/ 目录内容上传到静态托管服务
```

### GitHub Actions 自动部署

```yaml
# .github/workflows/deploy.yml
- 监听 push 到 main 分支
- 安装依赖
- 构建项目
- 部署到 GitHub Pages
```

## 🔄 扩展指南

### 添加新页面类型

1. 在 `src/types/page.ts` 定义新类型
2. 在 `src/components/pages/` 创建新组件
3. 在 `src/app/[slug]/page.tsx` 添加类型处理逻辑

### 添加新功能

1. 在 `src/lib/` 添加工具函数
2. 在 `src/components/` 创建组件
3. 在页面中使用新组件

### 自定义主题

1. 修改 `tailwind.config.mjs` 配置颜色
2. 更新 `src/app/globals.css` 自定义样式
3. 调整 `ThemeToggle.tsx` 主题切换逻辑

## 📚 相关文档

- [Next.js 文档](https://nextjs.org/docs)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [TypeScript 手册](https://www.typescriptlang.org/docs/)
- [React 文档](https://react.dev/)

## 🤝 贡献

查看 [CONTRIBUTING.md](../CONTRIBUTING.md) 了解如何参与项目开发。
